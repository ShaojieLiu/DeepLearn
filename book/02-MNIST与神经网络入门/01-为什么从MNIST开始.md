# 为什么从MNIST开始

因为它最适合做第一轮闭环。

你现在最缺的不是复杂模型，而是一次完整体验：

1. 把数据拿到
2. 把模型跑起来
3. 看到损失和准确率变化
4. 确认自己真的完成了一个分类任务

如果一开始就上更复杂的数据集，你会同时多出很多干扰：

1. 数据更复杂
2. 模型更复杂
3. 训练更慢
4. 调试更难

`MNIST` 的好处是，它把这些额外复杂度先压低了。

所以当前阶段选择 `MNIST`，不是因为它先进，而是因为它最适合建立第一轮真实认知。
